name: night_owl_prototype
description: A prototype app with Firebase email/password authentication.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.20.0
  firebase_auth: ^4.10.0
  cloud_firestore: ^4.10.0
  google_maps_flutter: ^2.4.9
  geolocator: ^10.4.0
  url_launcher: ^6.1.10
  cupertino_icons: ^1.0.5

flutter:
  uses-material-design: true
